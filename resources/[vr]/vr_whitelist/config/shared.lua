-- ============================================================
-- vr_whitelist — Konfiq
-- ============================================================

local config = {}

-- Sınaq müddəti (gün)
config.TrialDays = 14

-- Səviyyə sırası (aşağıdan yuxarı)
config.LevelOrder = { temporary = 1, citizen = 2, trusted = 3, veteran = 4 }

return config